# توثيق مشروع "صمود" (Somoud Project Documentation)

## نظرة عامة (Overview)

مشروع "صمود" هو منصة شاملة لإدارة الأزمات والطوارئ، تهدف إلى تقديم الدعم والإغاثة للمجتمع. يتكون المشروع من لوحة تحكم متقدمة (Admin Panel) مبنية بـ Laravel + Inertia.js + Vue 3، وواجهة برمجة تطبيقات (API) لربط تطبيقات الهاتف المحمول أو المواقع الإلكترونية الخارجية.

---

## ✨ المميزات الجديدة (New Features)

### 🔔 **إشعارات فورية لنداءات الاستغاثة**
- **تنبيهات صوتية تلقائية** عند استقبال نداء استغاثة جديد في لوحة التحكم
- **إشعارات المتصفح** (Browser Notifications) للتنبيه حتى عند عدم التركيز على النافذة
- **شارة مرئية** (Badge) في أسفل يسار الشاشة تظهر تلقائياً وتختفي بعد 10 ثوانٍ
- **تقنية Pusher** للبث المباشر (Real-time Broadcasting)
- **صوت تنبيه مخصص** (`sos.mp3`) يتم تشغيله تلقائياً

### 📊 **لوحة تحكم تفاعلية محسّنة**
- **بطاقات إحصائية ملونة** قابلة للنقر تعرض:
  - إجمالي نداءات الاستغاثة (قيد الانتظار / تم الحل)
  - قصص الصمود (منشور / قيد المراجعة)
  - بلوكات الإخلاء المسجلة
  - الاستشارات النفسية (قيد الانتظار)
  - منتجات السوق (النشطة)
- **جداول النشاط الأخير** تعرض:
  - آخر 5 نداءات استغاثة مع الحالة والموقع
  - آخر 5 قصص صمود مع النوع والحالة
- **تصميم عصري** مع دعم الوضع الليلي (Dark Mode)
- **روابط سريعة** للانتقال المباشر لصفحات الإدارة

### 🎨 **واجهة الإعدادات **
- **صفحة الملف الشخصي** (Profile)
- **صفحة كلمة المرور** (Password)
- **صفحة المظهر** (Appearance) مع خيارات:
  - فاتح (Light)
  - داكن (Dark)
  - تلقائي (System)
- **قائمة المستخدم** مترجمة مع خيارات الإعدادات وتسجيل الخروج
- **حذف الحساب** مع تحذيرات وتأكيدات 

## 1. لوحة التحكم (Admin Panel)

توفر لوحة التحكم واجهة سهلة الاستخدام  للمدراء والمشرفين لإدارة البيانات التالية:

### **أ. إدارة الإخلاء (Evacuation):**

- **بلوكات الإخلاء:** تقسيم المناطق إلى "بلوكات" وتحديد حالة كل بلوك (آمن، خطر، تحذير، تم الإخلاء).
- **حالات الإخلاء:** إدارة الحالات الممكنة وألوانها (مثلاً: الأحمر للخطر، الأخضر للآمن).

### **ب. السوق (Market):**

- **الأقسام:** إضافة وتعديل أقسام المنتجات (خضروات، معلبات، مستلزمات طبية، إلخ) مع صور أيقونات.
- **المنتجات:** إدارة المنتجات وأسعارها وحالة السعر (صاعد، هابط، مستقر) وأماكن توافرها.

### **ج. الدعم النفسي (Psychological Support):**

- **المراكز والمؤسسات:** دليل للمراكز النفسية، طرق التواصل، وحالة العمل (متاح/مغلق).
- **الاستشارات الخاصة:** استقبال وإدارة طلبات الاستشارات النفسية من المواطنين وتغيير حالتها (قيد الانتظار، جاري العمل...).

### **د. نداءات الاستغاثة (Distress Calls):**

- استقبال نداءات الطوارئ مع التفاصيل (الاسم، الموقع، الهاتف، الوصف).
- ترتيب النداءات تلقائياً حسب الأهمية (قيد الانتظار أولاً).
- **🔔 إشعارات فورية** صوتية ومرئية عند استقبال نداء جديد.
- **تكامل مع Pusher** للتحديثات الفورية.

### **هـ. قصص الصمود (Stories):**

- توثيق قصص ملهمة مع صور ونصوص غنية.
- **دعم الفيديو المضمّن** (YouTube) للتقارير.
- **حقل اسم الراوي** للإدارة الداخلية.
- إمكانية حفظ القصة كمسودة قبل نشرها للعامة.
- **تصميم محسّن** لصفحة التفاصيل.

### **و. إدارة النظام (System Management):**

- **المستخدمين:** إضافة مدراء ومشرفين للنظام.
- **الأدوار والصلاحيات (RBAC):** إنشاء أدوار مخصصة (مثل "مدير الطوارئ"، "مدير السوق") وتحديد صلاحياتهم بدقة لكل قسم في النظام.

---

## 2. دليل واجهة برمجة التطبيقات (API Documentation)

يمتلك المشروع API متكامل مبني باستخدام Laravel Sanctum للمصادقة.

### **المصادقة (Authentication):**

- **تسجيل الدخول (Login):**
    - **Endpoint:** `POST /api/login`
    - **Body:** `{ "email": "admin@example.com", "password": "password" }`
    - **Response:** يعيد `token` يجب استخدامه في الـ Headers للطلبات المحمية.

### **الهيكلية العامة للطلبات (Requests Structure):**

- **Base URL:** `https://your-domain.com/api`
- **Headers (للمسارات المحمية):**
    - `Authorization: Bearer <YOUR_TOKEN>`
    - `Accept: application/json`

---

### **أمثلة على الـ Endpoints:**

#### **1. الإخلاء (Evacuation)**

- **الحصول على حالة البلوكات:**
    - `GET /api/evacuation/blocks`
    - **الوصف:** يعيد قائمة بجميع مناطق الإخلاء وحالتها الحالية (آمن/خطر).

#### **2. السوق (Market)**

- **جلب الأقسام (للتطبيق):**
    - `GET /api/market/categories`
    - **ملاحظة:** يعيد فقط الأقسام المفعلة (Active).

- **جلب المنتجات:**
    - `GET /api/market/products`
    - **مثال الاستجابة:**
        ```json
        [
            {
                "id": 1,
                "name": "طحين",
                "price": 50,
                "price_status": "rising",
                "category": { "id": 2, "name": "مواد غذائية" }
            }
        ]
        ```

#### **3. الدعم النفسي (Psychological Support)**

- **إرسال طلب استشارة (عام):**
    - `POST /api/psychological/consultations`
    - **Body:**
        ```json
        {
            "client_name": "أحمد محمد",
            "phone_number": "0599xxxxxx",
            "title": "قلق وتوتر شديد",
            "description": "أعاني من صعوبة في النوم بسبب الأصوات..."
        }
        ```

#### **4. نداءات الاستغاثة (Distress Calls)**

- **إرسال نداء استغاثة (عام):**
    - `POST /api/distress-calls`
    - **Body:**
        ```json
        {
            "name": "سعيد خليل",
            "phone_number": "0590000000",
            "detailed_address": "حي الرمال، شارع الوحدة، عمارة 5",
            "description": "نحتاج سيارة إسعاف فوراً، يوجد مصاب",
            "status": "pending"
        }
        ```
    - **ملاحظة:** يتم تعيين الحالة تلقائياً إلى "pending" (قيد الانتظار) إذا لم ترسلها، وهي الحالة ذات الأولوية القصوى في لوحة التحكم.
    - **🔔 يتم إرسال إشعار فوري** للوحة التحكم عند استقبال نداء جديد.

#### **5. قصص الصمود (Stories)**

- **جلب القصص المنشورة:**
    - `GET /api/stories/public`
    - **الوصف:** يعيد القصص التي تم اعتمادها ونشرها من لوحة التحكم فقط.
    - **يدعم:** النصوص، الصور، والفيديوهات المضمّنة.

---

## 3. التقنيات المستخدمة (Tech Stack)

### **Backend:**
- Laravel 11.x
- PHP 8.2+
- MySQL/PostgreSQL
- Laravel Sanctum (API Authentication)
- Pusher (Real-time Broadcasting)
- Spatie Laravel Permission (RBAC)

### **Frontend (Admin Panel):**
- Vue 3 (Composition API)
- Inertia.js
- TypeScript
- Tailwind CSS
- Radix Vue (UI Components)
- Lucide Icons

### **Frontend (Public):**
- Laravel Blade Templates
- Bootstrap 5
- Vanilla JavaScript

---

## 4. الإعداد والتثبيت (Setup & Installation)

### **المتطلبات:**
- PHP >= 8.2
- Composer
- Node.js >= 18.x
- NPM or Yarn
- MySQL/PostgreSQL

### **خطوات التثبيت:**

```bash
# 1. استنساخ المشروع
git clone https://github.com/Eng-Ma/Somoud.git
cd Somoud

# 2. تثبيت الحزم
composer install
npm install

# 3. نسخ ملف البيئة
cp .env.example .env

# 4. توليد مفتاح التطبيق
php artisan key:generate

# 5. إعداد قاعدة البيانات في .env
# DB_DATABASE=somoud
# DB_USERNAME=root
# DB_PASSWORD=

# 6. إعداد Pusher في .env
# BROADCAST_CONNECTION=pusher
# PUSHER_APP_ID=your_app_id
# PUSHER_APP_KEY=your_app_key
# PUSHER_APP_SECRET=your_app_secret
# PUSHER_APP_CLUSTER=your_cluster

# 7. تشغيل الترحيلات
php artisan migrate --seed

# 8. بناء الأصول
npm run build

# 9. تشغيل السيرفر
php artisan serve
```

---

## 5. ملاحظات هامة للمطورين (Notes for Developers)

1.  **الصور والملفات:**
    - عند رفع صور (مثلاً لإضافة قصة أو قسم جديد)، يجب استخدام `Content-Type: multipart/form-data`.
    - في حالة **تحديث** (Update) مورد يحتوي على ملفات، استخدم `POST` مع إضافة حقل `_method` بقيمة `PUT` في الـ Body.

2.  **التواريخ:**
    - جميع التواريخ تعاد بصيغة ISO 8601 القياسية (e.g., `2026-01-09T10:00:00.000000Z`).

3.  **إدارة الأخطاء:**
    - في حالة وجود أخطاء في البيانات (Validation Error)، يعيد السيرفر كود `422` مع تفاصيل الحقول الخاطئة.
    - في حالة عدم الصلاحية (Unauthorized)، يعيد `401`.
    - في حالة عدم وجود الصلاحية للدور (Forbidden)، يعيد `403`.

4.  **Pusher Configuration:**
    - تأكد من إعداد Pusher بشكل صحيح في `.env` لتفعيل الإشعارات الفورية.
    - ملف الصوت `sos.mp3` يجب أن يكون موجوداً في `public/sos.mp3`.

5.  **Build Process:**
    - استخدم `npm run dev` للتطوير مع Hot Module Replacement.
    - استخدم `npm run build` للإنتاج.

---

## 6. الترخيص (License)

هذا المشروع مفتوح المصدر ومتاح للاستخدام والتطوير.

---

## 7. الدعم والمساهمة (Support & Contribution)

للإبلاغ عن المشاكل أو المساهمة في التطوير، يرجى فتح Issue أو Pull Request على GitHub.

**Repository:** 

---

**تم التحديث:** 2026-01-09
**الإصدار:** 1.0.0

